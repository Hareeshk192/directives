import { DnameDirective } from './dname.directive';

describe('DnameDirective', () => {
  it('should create an instance', () => {
    const directive = new DnameDirective();
    expect(directive).toBeTruthy();
  });
});
